#pragma once

// --- FSGL ---
#include "KeyCodes.hpp"



namespace fsgl
{
    namespace input
    {
        // Check if a key is currently being pressed
        bool isKeyPressed(const KeyCode code);
    }
}
